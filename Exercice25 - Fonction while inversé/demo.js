function wholeNumberReversed(n)
{
let compteur = n

    while(compteur > 0)
    {
        console.log(compteur)
        compteur--
    }
};

wholeNumberReversed(Number(prompt("Entrez un chiffre : ")));